# Production environment variables for Vercel
# Point to your LOCAL push server (you'll need to expose it via ngrok or similar)
# VITE_PUSH_API_URL will be set in Vercel dashboard as environment variable
